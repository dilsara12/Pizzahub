import { Text, View } from 'react-native';
import React from 'react';

const screenB = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 30, color: '#OOO' }}> Welcome Screen B</Text>
    </View>
  );
};

export default screenB;
